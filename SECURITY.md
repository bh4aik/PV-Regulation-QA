# 安全说明

## 部署要求

- 使用 Node.js 22 或 Docker Compose。
- 公网只开放 80/443；应用端口默认绑定 `127.0.0.1:3000`，由 Caddy 反向代理。
- 设置强管理员密码、`AUTO_FIRST_ADMIN=false`，并按需启用邀请码和 Turnstile。
- `data/state/` 含 API Key、会话、账号信息与问答历史，应限制权限并加密备份。
- 本项目的 JSON 状态存储只面向单实例、小规模部署；多实例部署应迁移到支持事务的数据库。

## 已实施的保护

- 上传、清洗和入库仅限管理员；上传大小限制为 30 MB。
- 法规 ID 采用白名单校验，阻止目录穿越。
- 爬虫限制为公网 HTTP(S)，校验每次重定向和 DNS 结果，并限制响应体为 5 MB。
- 问答限制为 2000 字、每 IP 每分钟 20 次，并阻止并发请求绕过每日额度。
- 外部 API 请求设置超时；敏感 Key 不通过设置接口回显。
- JSON 状态使用临时文件加原子重命名写入，权限设置为 `0600`。
- 容器以非 root 用户运行、根文件系统只读、移除 Linux capabilities，并禁止提权。

## 报告问题

请通过仓库的私密安全报告渠道提交漏洞，不要在公开 Issue 中附带 API Key、会话令牌、用户数据或服务器地址。
